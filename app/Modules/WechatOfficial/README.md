# 微信公众号模块

站点模块，按当前系统模块标准落地，统一走模块注册、站点绑定、试用、停用和移除清理机制。

当前执行方案：

- 第一阶段：模块骨架、后台统一风格页面、权限、配置存储、独立数据表
- 第二阶段：菜单管理与公众号菜单同步
- 第三阶段：系统文章推送、草稿生成、发布记录
- 第四阶段：素材管理、接口日志、失败重试

强制要求：

- 后台页面风格必须和现有系统统一，不允许单独再做一套后台 UI
- 所有模块数据必须按 `site_id` 隔离
- 模块移除时只能清理当前站点数据
- 模块停用仅拦后台，不直接影响前台已发布内容
